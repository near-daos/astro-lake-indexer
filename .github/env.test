
NODE_ENV=development

# Lines with HELM_ prefix will appear in container as env vars
HELM_REDIS_STREAMING=true

HELM_START_BLOCK_HEIGHT=50043993
HELM_LOOK_BACK_BLOCKS=20
HELM_TX_CACHE_SIZE=1000
HELM_TX_HASHES_CACHE_SIZE=10000

HELM_FETCH_MAX_KEYS=100
HELM_BLOCKS_DL_CONCURRENCY=50
HELM_WAIT_FOR_NEW_BLOCKS=2000
BLOCK_FETCH_RETRIES=40
BLOCK_FETCH_DELAY_MS=1000

HELM_TRACK_ACCOUNTS=sputnikv2.testnet
HELM_AWS_BUCKET=near-lake-data-testnet

HELM_DATADOG_APM_ENABLED=true
HELM_DATADOG_ENV=test
